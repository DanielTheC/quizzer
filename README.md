# Quizzer

- **Mobile app (Expo React Native)** — `apps/app` (internal EAS builds: see `apps/app/INTERNAL_TESTING.md`)
- **Marketing website** — `apps/website` (to be added)

Supabase config and migrations live at the repo root in `supabase/`.
