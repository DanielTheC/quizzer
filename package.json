{
  "name": "quizzer-monorepo",
  "private": true,
  "scripts": {
    "start": "npm start --prefix apps/app",
    "start:clean": "npm run start:clean --prefix apps/app",
    "android": "npm run android --prefix apps/app",
    "ios": "npm run ios --prefix apps/app",
    "web": "npm run web --prefix apps/app"
  }
}
